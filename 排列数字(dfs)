#include<iostream>
#include<vector>

using namespace std;

vector<vector<int>> res;
vector<int> path;

void back(int n, vector<int>& use) {
    if(path.size() == n) {
        res.push_back(path);
        return;
    }
    for(int i = 1; i <= n; i++) {
        if(use[i]) {
            continue;
        }
        use[i] = 1;
        path.push_back(i);
        back(n, use);
        path.pop_back();
        use[i] = 0;
    }
}
int main() {
    int n;
    scanf("%d", &n);
    vector<int> use(n + 1);
    back(n, use);
    for(int i = 0; i < res.size(); i++) {
        for(int j = 0; j < n; j++) {
            cout << res[i][j] << ' ';
        }
        cout << endl;
    }
    return 0;
}
