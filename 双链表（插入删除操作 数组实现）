#include<iostream>

using namespace std;

const int N = 1e5 + 10;

int m, k, x;

int idx;
int e[N], l[N], r[N];

void init()
{
    r[0] = 1;
    l[1] = 0;
    idx = 2;
}

void r_insert(int k, int x)
{
    e[idx] = x;
    r[idx] = r[k];
    l[idx] = k;
    l[r[k]] = idx;
    r[k] = idx ++;
}

void remove(int k)
{
    r[l[k]] = r[k];
    l[r[k]] = l[k];
}
int main()
{
    cin >> m;
    init();
    while(m -- )
    {
        string c;
        cin >> c;
        if(c == "R")
        {
            cin >> x;
            r_insert(l[1], x);
        }
        else if(c == "L")
        {
            cin >> x;
            r_insert(0, x);
        }
        else if(c == "D")
        {
            cin >> k;
            remove(k + 1);
        }
        else if(c == "IR")
        {
            cin >> k >> x;
            r_insert(k + 1, x);
        }
        else if(c == "IL")
        {
            cin >> k >> x;
            r_insert(l[k + 1], x);
        }
    }
    for(int i = r[0]; i != 1; i = r[i]) cout << e[i] << ' ';
    return 0;
}
